<% if current_user.following?(@task) %>
	<%= link_to "Follow", task_follow_path(@task), :method => 'put', :remote => true, :class => "btn btn-sm btn-following" %>
<% else %>
	<%= link_to "Follow", task_follow_path(@task), :method => 'put', :remote => true, :class => "btn btn-sm btn-follow" %>
<% end %>