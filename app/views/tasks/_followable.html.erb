<% if current_user.follow?(@task) %>
	<%= link_to "Following", task_follow_path(@task), :method => 'put', :remote => true, :class => "btn btn-sm btn-following" %>
<% else %>
	<%= link_to "Follow", task_follow_path(@task), :method => 'put', :remote => true, :class => "btn btn-sm btn-follow" %>
<% end %>